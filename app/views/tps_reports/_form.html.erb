<%= form_for(@tps_report) do |f| %>
  <% if @tps_report.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tps_report.errors.count, "error") %> prohibited this tps_report from being saved:</h2>

      <ul>
      <% @tps_report.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :report %><br />
    <%= f.text_area :report %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
